"""This module contains the service for the LLM chat."""


def send_message(input: str) -> str:
    """Send a message to the chat.

    Args:
        input: The message to send to the LLM.

    Returns:
        The message to send to the LLM.
    """
    return input